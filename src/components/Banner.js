import React from 'react'

const Banner = (props) => {



  return (
    <div>
        <p>{props.heading}</p>
        Banner
    <button onClick={props.heading}>Click here</button>
    </div>
  )
}

export default Banner